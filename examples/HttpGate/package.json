{
  "name": "gate",
  "version": "1.0.0",
  "description": "",
  "main": "dist/examples/HttpGate/start.mono.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildMono": "rm -rf ./dist && npx tsc --project tsconfig.mono.json",
    "buildMicro": "rm -rf ./dist && npx tsc --project tsconfig.micro.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.3.0"
  }
}
